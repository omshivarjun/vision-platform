// Export all models from the backend directory
export { User, type IUser, type UserDocument, type UserProfile } from './User';

// Re-export types for convenience
export type { IUser as UserInterface } from './User';
