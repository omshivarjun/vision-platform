describe('Mobile App Basic Test', () => {
  it('should pass a basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should handle strings', () => {
    expect('mobile').toBe('mobile');
  });
});
